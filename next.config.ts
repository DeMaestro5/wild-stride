/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // This disables the Next.js logo/icon in development mode
  devIndicators: {
    buildActivity: false,
  },
};

module.exports = nextConfig;
